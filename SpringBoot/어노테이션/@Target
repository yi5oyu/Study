https://docs.oracle.com/javase/8/docs/api/index.html?java/lang/annotation/ElementType.html

어노테이션이 붙을 수 있는 타입 지정

@Target(ElementType.TYPE)

ElementType에 정의되 있는 Enum 상수(Constants)

ANNOTATION_TYPE
 - 어노테이션 유형에 적용(다른 어노테이션에 어노테이션 추가)

CONSTRUCTOR
 - 생성자에 적용(주입, 로깅)

FIELD
 - 필드, Enum 상수에 적용(주입, 검증, 직렬화)

LOCAL_VARIABLE
 - 지역변수에 적용(거의 사용되지 않음)

METHOD
 - 메소드에 적용(HTTP 요청 매핑, AOP, 로깅...)

PACKAGE
 - 패키지 선언에 적용(package-info.java)

PARAMETER
 - 메서드, 생성자의 매개변수에 적용(주입, 검증, 문서화)

TYPE
 - 클래스, 인터페이스, Enum형에 적용

TYPE_PARAMETER
 - 타입 매개변수에 적용

TYPE_USE
 - 타입이 사용되는 모든 곳에 적용 가능
