Lombok
 - 필수 메서드 등을 지원해 코드를 간소화 해주는 라이브러리
 - 리팩토링

@Builder
 빌더 패턴을 사용해 객체 생성할 수 있음
@NoArgsConstructor
 기본 생성자 생성해줌
@AllArgsConstructor
 모든 필드를 인자로 받는 생성자를 생성
@RequiredArgsConstructor
 선언된 모든 final필드가 포함된 생성자 생성
@ToString
 toString() 메서드
@Slf4j 
 (Simple Logging Facade for java)
 로깅 기능 : 컨트롤러에 로그 남김, 어떤 데이터가 언제 저장됐는지 시간 정보도 볼 수 있음
 log.info() 문으로 출력하기 원하는 값을 넣으면 됨
@Getter
@Setter
