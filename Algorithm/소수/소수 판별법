2,3으로 나누어 떨어지는 수는 소수가 아님
5부터 시작해서 n의 제곱근까지 6k±1의 수로 나누어 떨어지는지 확인

2와 3을 제외한 모든 소수는 6k±1
6k±1의 수 중에서 소수 판별
25미만의 6k±1의 수는 모두 소수
 - 5 7 11 13 17 19 23

6k±1의 수 중에 작은 소수로 나눠지지 않는 수를 찾아야함

나눠지는 모든 수들은 짝을 가짐
 - 제곱근의 수 하나만 찾으면 됨
 - 36) 1,36 / 2,18 / 3,12 / 4,9 / 6,6

6k±1 가 n 일때 n이 보다 작은 소수로 나눠지는지 확인
for(int i = 5 ; i*i <= n ; i+=6)
 if(i % n == 0 || (i+2) % n == 0)
    = 소수 아님
