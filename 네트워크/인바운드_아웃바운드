인바운드(Inbound): 외부에서 내부로 들어오는 트래픽/연결
 - 인터넷 > 내 서버/네트워크
   (웹사이트 방문자가 내 서버로 HTTP 요청, 외부에서 SSH로 서버 접속, 수신)

아웃바운드(Outbound): 내부에서 외부로 나가는 트래픽/연결
 - 내 서버/네트워크 > 인터넷
   (서버에서 외부 API 호출, 소프트웨어 업데이트 다운로드, 발송)
