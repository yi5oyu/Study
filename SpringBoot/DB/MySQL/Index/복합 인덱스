복합 인덱스
 장점
  두 개 이상의 열을 사용한 검색 조건을 빠르게 처리
  중복된 데이터 행을 제거
  정렬하거나 그룹화할 때 속도 향상

 인덱스 순서 중요 
  인덱스가 생성되는 순서에 따라 정렬
  첫 번째 컬럼이 가장 중요한 역할 (쿼리에 첫 번째 칼럼이 없다면 복합 인덱스 검색 안됨)
  자주 이용되는 순서대로 결합 인덱스 컬럼의 순서 결정
   - WHERE문에 많이 사용되는 칼럼 -> Equal(=)로 사용되는 칼럼 -> 분포도가 좋은 칼럼

 너무 많은 인덱스로 인해 성능을 저하 일어날 수 있음, 데이터 삽입 및 업데이트 시 추가적인 비용이 발생할 수 있음

* 분포도
 - 칼럼의 값들이 데이터베이스 전체에 고르게 분포되어 있어서 특정 값이나 소수의 값들에 편중되지 않은 칼럼
  분포도가 좋은 칼럼 찾는 방법
   1. 카디널리티(Cardinality): 서로 다른 값의 개수
     - SELECT COUNT(DISTINCT 칼럼명_1) AS 별칭 FROM 테이블명;
     카디널리티 ∝ 분포도
   2. 분포 히스토그램
     MySQL 8.0 이상: ANALYZE TABLE 명령어 사용 (칼럼에 대한 히스토그램 업데이트)
      - ANALYZE TABLE 테이블명 UPDATE HISTOGRAM ON 칼럼명_1, 칼럼명_2;
      히스토그램 데이터 조회
      - SELECT * FROM information_schema.COLUMN_STATISTICS WHERE SCHEMA_NAME = DB이름 AND TABLE_NAME = 테이블명;
