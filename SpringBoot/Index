단일 인덱스

MySQL
  CREATE INDEX idx_칼럼이름_1 ON product (칼럼이름_1);
  CREATE INDEX idx_칼럼이름_2 ON product (칼럼이름_2);

엔티티 어노테이션 추가
@Table(name = "테이블명", indexes = {
        @Index(name = "idx_칼럼이름_1", columnList = "칼럼이름_1"),
        @Index(name = "idx_칼럼이름_2", columnList = "칼럼이름_2")
})

